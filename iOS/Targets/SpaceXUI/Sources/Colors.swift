import Foundation
import SwiftUI

public struct SpaceXColor {
    
    static public let primary = Color(.primary)
    static public let background = Color(.background)
    static public let elevatedBackground = Color(.elevatedBackground)
    static public let primaryText = Color(.primaryText)
    static public let secondaryText = Color(.secondaryText)
    static public let opaqueText = Color(.opaqueText)
}
